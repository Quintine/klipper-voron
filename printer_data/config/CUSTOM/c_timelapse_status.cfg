[timelapse_status]

[gcode_macro SHOW_TIMELAPSE]
gcode:
    {% set tl = printer["timelapse_status"].enabled %}
    {% set msg = "🎥 Timelapse is " ~ ("ON" if tl else "OFF") %}
    RESPOND MSG="{msg}"