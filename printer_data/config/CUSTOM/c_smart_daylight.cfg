[gcode_macro SMART_DAYLIGHT]
description: Run EVENING_DAYLIGHT only if timelapse is enabled (no messages)
gcode:
    {% if printer["gcode_macro TIMELAPSE_TAKE_FRAME"].enable %}
        EVENING_DAYLIGHT
    {% endif %}