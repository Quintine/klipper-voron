[gcode_macro SMART_DAYLIGHT]
description: Adjust daylight only if timelapse is enabled
gcode:
    {% if printer["timelapse_status"].enabled %}
        RESPOND MSG="🎥 Timelapse active — adjusting daylight"
        EVENING_DAYLIGHT
    {% else %}
        G4 P0
    {% endif %}